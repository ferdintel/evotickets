"use client";

import ProtectedRoute from "components/ProtectedRoute";

const Profile = () => {
  return (
    <main>
      <h1>Profile</h1>
    </main>
  );
};

export default ProtectedRoute(Profile);
