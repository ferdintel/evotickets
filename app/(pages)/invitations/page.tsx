"use client";

import ProtectedRoute from "components/ProtectedRoute";

const Invitations = () => {
  return (
    <main>
      
      <h1>Invitations</h1>
    </main>
  );
};

export default ProtectedRoute(Invitations);
